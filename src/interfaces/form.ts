/**
 * Interface for the signup form fielappService
 */
export interface SignupFormFielappService {
    name: string;
    email: string;
    password: string;
    confirmPassword: string;
}


/**
 * Interface for the login form fielappService
 */
export interface LoginFormFielappService {
    email: string;
    password: string;
}
